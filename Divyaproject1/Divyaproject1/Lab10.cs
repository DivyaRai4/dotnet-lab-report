using System;

class VowelConsonantChecker
{
    /*
    static void Main()
    {
        // Taking input from the user
        Console.Write("Enter an alphabet: ");
        char ch = Convert.ToChar(Console.ReadLine().ToLower()); // Convert to lowercase to handle both uppercase and lowercase letters

        // Using a switch statement to check if the alphabet is a vowel or consonant
        switch (ch)
        {
            case 'a':
            case 'e':
            case 'i':
            case 'o':
            case 'u':
                Console.WriteLine($"{ch} is a vowel.");
                Console.ReadLine();

                break;

            default:
                // Check if the input is a valid alphabet letter
                if (char.IsLetter(ch))
                {
                    Console.WriteLine($"{ch} is a consonant.");
                    Console.ReadLine();
                }
                else
                {
                    Console.WriteLine($"{ch} is not a valid alphabet letter.");
                    Console.ReadLine();
                }
                break;
        }
    } */
}
